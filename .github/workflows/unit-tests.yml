name: Unit Tests
on: [ pull_request ]
jobs:
  jest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: pnpm/action-setup@v4
      - name: Test
        run: pnpm test:unit
        with:
          version: 8
